# Тесты для trade_bot

## Установка зависимостей

```bash
pip install -r requirements.txt
```

## Запуск тестов

```bash
# Запуск всех тестов
pytest

# Запуск с подробным выводом
pytest -v

# Запуск конкретного файла
pytest tests/test_sync_orders.py

# Запуск конкретного теста
pytest tests/test_sync_orders.py::TestProcessUserOrders::test_reposition_sufficient_change
```

## Структура тестов

### test_sync_orders.py

Тесты для модуля `bot/sync_orders.py`, покрывающие:

1. **TestCalculateNewTargetPrice** - тесты функции расчета целевой цены:
   - Расчет для BUY ордеров
   - Расчет для SELL ордеров
   - Ограничения минимальной и максимальной цены

2. **TestProcessUserOrders** - тесты основной функции обработки ордеров:
   - Пользователь не найден
   - Нет активных ордеров
   - Изменение достаточно для перестановки
   - Изменение недостаточно для перестановки
   - Цена не изменилась
   - Несколько ордеров (смешанный случай)
   - Уведомления отправляются всегда
   - Проверка структуры уведомлений

## Покрытие кейсов

### ✅ Изменение достаточно для перестановки
- Ордер добавляется в списки для отмены и размещения
- Уведомление содержит `will_reposition: True`
- Параметры нового ордера корректны

### ⏸️ Изменение недостаточно
- Ордер НЕ добавляется в списки
- Уведомление все равно отправляется с `will_reposition: False`

### 📨 Уведомления
- Уведомления отправляются всегда (независимо от решения о перестановке)
- Уведомления содержат все необходимые поля
- Корректные значения в уведомлениях

### 🔄 Перестановка ордеров
- Правильные списки для отмены
- Правильные параметры для размещения новых ордеров
- Корректная обработка нескольких ордеров

